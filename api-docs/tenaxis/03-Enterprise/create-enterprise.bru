meta {
  name: Create Enterprise
  type: http
  seq: 1
}

post {
  url: {{local}}/enterprise/create
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "nombre": "Control de Plagas Norte",
    "activo": true
  }
}
