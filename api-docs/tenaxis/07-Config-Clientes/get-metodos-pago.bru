meta {
  name: Get Metodos Pago
  type: http
  seq: 14
}

get {
  url: {{local}}/config-clientes/metodos-pago?empresaId={{enterpriseId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
