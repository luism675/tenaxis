meta {
  name: Get My Follow Up Status
  type: http
  seq: 5
}

get {
  url: {{local}}/ordenes-servicio/follow-ups/my-status
  body: none
  auth: bearer
}

params:query {
  empresaId: {{enterpriseId}}
}

auth:bearer {
  token: {{token}}
}
