meta {
  name: Delete Orden Servicio
  type: http
  seq: 15
}

delete {
  url: {{local}}/ordenes-servicio/{{ordenId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "reason": "Solicitud duplicada"
  }
}
