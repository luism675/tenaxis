meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{local}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "santiago.echeverry@tecnico.com",
    "password": "123456"
  }
}

script:post-response {
  if (res.body?.data?.access_token) {
    bru.setVar("token", res.body.data.access_token);
  }
}
